{
  "builds": [
    {
      "src": "js/*.js",
      "use": "@vercel/static-build",
      "config": {
        "public": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
